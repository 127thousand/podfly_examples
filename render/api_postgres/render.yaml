# Generated by podfly — Render Blueprint (IaC)
# https://render.com/docs/blueprint-spec
# Monorepo: set rootDir so the example need not be repo root.
services:
  - type: web
    name: podfly-render-api-pg
    runtime: docker
    plan: free
    region: oregon
    rootDir: render/api_postgres
    dockerfilePath: ./render_api_pg_server/Dockerfile
    dockerContext: .
    healthCheckPath: /
    envVars:
      - key: runmode
        value: production
      - key: SERVERPOD_RUN_MODE
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: podfly-render-api-pg-db
          property: connectionString
databases:
  - name: podfly-render-api-pg-db
    plan: free
    region: oregon
