# render_web_demo_server

This is the starting point for your Serverpod server.

Start your server by running:

    cd render_web_demo
    serverpod start

When you are finished, you can shut down the running server with `Q`.
