# Generated by podfly — Render Blueprint (IaC)
# https://render.com/docs/blueprint-spec
# Monorepo: set rootDir so the example need not be repo root.
services:
  - type: web
    name: podfly-render-web-api
    runtime: docker
    plan: free
    region: oregon
    rootDir: render/api_and_static
    dockerfilePath: ./render_web_demo_server/Dockerfile
    dockerContext: .
    healthCheckPath: /
    envVars:
      - key: runmode
        value: production
      - key: SERVERPOD_RUN_MODE
        value: production
