# ECS monolith: nginx on $PORT (8080) → static Flutter + proxy to API :8081
# publicHost is patched by podfly after first deploy (ALB DNS).
apiServer:
  port: 8081
  publicHost: localhost
  publicPort: 80
  publicScheme: http

sessionLogs:
  consoleEnabled: true
  persistentEnabled: false
