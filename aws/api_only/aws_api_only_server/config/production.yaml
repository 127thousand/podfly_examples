# App Runner: container listens on 8080 (ImageConfiguration.Port).
# publicHost is patched by podfly after first deploy.
apiServer:
  port: 8080
  publicHost: localhost
  publicPort: 443
  publicScheme: https

sessionLogs:
  consoleEnabled: true
  persistentEnabled: false
