# Vercel examples

Static Flutter web via **`web_host: vercel`** (same role as Cloudflare Pages).

| Path | API | UI |
|------|-----|-----|
| [split_fly](split_fly/) | Fly.io | Vercel |

API + WebSockets never run on Vercel — only the Flutter web build.
