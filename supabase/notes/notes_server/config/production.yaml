# Patched by podfly on deploy (api publicHost + Supabase session pooler).
apiServer:
  port: 8080
  publicHost: localhost
  publicPort: 443
  publicScheme: https

sessionLogs:
  consoleEnabled: true
  persistentEnabled: false
