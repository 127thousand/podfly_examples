# Container Apps monolith: nginx on 8080 → static Flutter + proxy to API :8081
# publicHost is patched by podfly after first deploy (FQDN).
apiServer:
  port: 8081
  publicHost: localhost
  publicPort: 443
  publicScheme: https

sessionLogs:
  consoleEnabled: true
  persistentEnabled: false
